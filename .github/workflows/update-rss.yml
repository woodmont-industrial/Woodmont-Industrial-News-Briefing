name: Update RSS Feed

# Workflow disabled - uncomment to re-enable
# on:
#   schedule:
#     # Run every 30 minutes (you can adjust this)
#     - cron: '*/30 * * * *'
#   workflow_dispatch:
#     # Allow manual triggering
#   push:
#     branches:
#       - main
#     paths:
#       - 'docs/index.html'

on:
  workflow_dispatch:
    # Manual triggering only

jobs:
  update-feed:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        # Fetch full history for proper git operations
        fetch-depth: 0

    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'
        cache: 'npm'

    - name: Install dependencies
      run: npm ci

    - name: Fetch RSS feeds and build static files
      run: npm run build-static
      env:
        NODE_ENV: production

    - name: Commit and push changes
      run: |
        git config --local user.email "action@github.com"
        git config --local user.name "GitHub Action"
        git add docs/
        git diff --quiet && git diff --staged --quiet || git commit -m "Update RSS feed and static files [$(date +'%Y-%m-%d %H:%M UTC')]"
        git push
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}